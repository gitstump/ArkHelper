# Dependencies
node_modules/

# Generated/runtime data — these are regenerated by running the services,
# not source code, and differ per-machine
roster.json
ark_history.db
ark_history.db-*
ark_tools.db
ark_tools.db-*
*.db
*.db-shm
*.db-wal

# Local environment secrets — never commit tokens/keys
.env
.env.local

# OS junk
.DS_Store
Thumbs.db
